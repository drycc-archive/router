apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    router.drycc.cc/routable: "true"
    app: nginx
  annotations:
    router.drycc.cc/domains: nginx
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
